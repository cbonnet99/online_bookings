<%= client.default_name%> <span class="email"><br/><%= link_to "Send an email to #{client.default_name}", "mailto:#{client.email}"%></span><%unless client.no_phone_number?%> | <span class="phone"><%= client.phone%></span><%end%> | <%= link_to "Delete client", practitioner_client_url(:id => client.id, :practitioner_id => current_pro.permalink), :method => :delete%>